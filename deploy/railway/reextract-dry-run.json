{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "npm ci && npm run build"
  },
  "deploy": {
    "startCommand": "node dist/scripts/reextract-recategorize.js --dry-run --all",
    "restartPolicyType": "NEVER"
  }
}
